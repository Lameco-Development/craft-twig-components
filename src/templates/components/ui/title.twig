{% set level = attributes.level ?: 'strong' %}
<{{ level }} {{ attributes.merge({ class: 'block leading-normal title-fade-in' }) }}>{{ slot | striptags(attributes.stripTags ?? '<strong>') | raw }}</{{ level }}>
