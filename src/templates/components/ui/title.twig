{% set level = attributes.level ?: 'strong' %}
<{{ level }} class="{{ attributes.merge({ class: 'block leading-normal title-fade-in' }).class }}">{{ slot | striptags(attributes.stripTags ?? '<strong>') | raw }}</{{ level }}>
