{% set level = attributes.level ?: 'strong' %}
<{{ level }} class="{{ attributes.merge({ class: 'block title-fade-in' }).class }}">{{ slot | striptags(attributes.stripTags ?? '<strong><em><p>') | raw }}</{{ level }}>
